apiVersion: v1
kind: Service
metadata:
  name: multicluster-controlplane
  labels:
    component: multicluster-controlplane
spec:
  type: NodePort
  selector:
    app: multicluster-controlplane
  ports:
    - protocol: TCP
      port: 443
      targetPort: 9443
      nodePort: 30080